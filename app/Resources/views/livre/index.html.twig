{% extends 'base.html.twig' %}

{% block body %}
    <h1>Livres list</h1>

    <table>
        <thead>
            <tr>
                <th>Auteur</th>
                <th>Id</th>
                <th>Titre</th>
                <th>Description</th>
                <th>Langue</th>
                <th>Prix</th>
                <th>Datepub</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for livre in livres %}
            <tr>
                <td><a href="{{ path('livre_show', { 'id': livre.id }) }}">{{ livre.auteur }}</a></td>
                <td>{{ livre.id }}</td>
                <td>{{ livre.titre }}</td>
                <td>{{ livre.description }}</td>
                <td>{{ livre.langue }}</td>
                <td>{{ livre.prix }}</td>
                <td>{% if livre.datePub %}{{ livre.datePub|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('livre_show', { 'id': livre.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('livre_edit', { 'id': livre.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('livre_new') }}">Create a new livre</a>
        </li>
    </ul>
{% endblock %}
