{% extends 'base.html.twig' %}

{% block body %}
    <h1>Activite creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input CLASS="btn btn-block btn-success" type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>

            <a CLASS="btn btn-block bg-gradient-danger" href="{{ path('activite_index') }}">Back to the list</a>

    </ul>

{% endblock %}
